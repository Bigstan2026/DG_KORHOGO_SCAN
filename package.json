{
  "name": "scan-codes-stan",
  "version": "1.0.0",
  "description": "Scan de codes-barres avec export Excel - STAN DISTRIBUTION",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
